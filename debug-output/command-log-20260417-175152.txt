git rev-parse --abbrev-ref HEAD; git status --short; git remote -v
git fetch origin; git checkout blender-solved-diagnostics-handoff; git pull --ff-only origin blender-solved-diagnostics-handoff; git rev-parse --abbrev-ref HEAD
try { . "c:\Users\abdul\AppData\Local\Programs\Microsoft VS Code\cfbea10c5f\resources\app\out\vs\workbench\contrib\terminal\common\scripts\shellIntegration.ps1" } catch {}
$candidates = @("D:\blender.exe", "D:\Blender.exe", "D:\blender", "D:\blender\blender.exe", "D:\Blender\blender.exe"); $found = @(); foreach ($c in $candidates) { if (Test-Path $c) { $item = Get-Item $c; if ($item.PSIsContainer) { $exe = Join-Path $item.FullName "blender.exe"; if (Test-Path $exe) { $found += $exe } } else { $found += $item.FullName } } }; if (-not $found) { $found += (Get-ChildItem "D:\" -Filter "*blender*.exe" -File -ErrorAction SilentlyContinue | Select-Object -ExpandProperty FullName) }; $found = $found | Select-Object -Unique; if (-not $found -or $found.Count -eq 0) { Write-Output "BLENDER_NOT_FOUND_ON_D_ROOT"; exit 1 }; $blenderExe = $found[0]; Write-Output "BLENDER_EXE=$blenderExe"; & $blenderExe --version
Write-Output "PRIMARY_FILES"; rg --files -g "**/blender_yolo_generator_v2.py" -g "**/blender_solved_diagnostics.py" -g "**/copilot-continuation-handoff.md" -g "**/copilot-next-commands.md"; $scenePath = ""; if (Test-Path "scene.blend") { $scenePath = (Resolve-Path "scene.blend").Path; Write-Output "SCENE_SELECTION_RULE=preferred_scene_blend" } else { $latestBlend = Get-ChildItem -Recurse -Filter "*.blend" -File -ErrorAction SilentlyContinue | Sort-Object LastWriteTime -Descending | Select-Object -First 1; if ($latestBlend) { $scenePath = $latestBlend.FullName; Write-Output "SCENE_SELECTION_RULE=latest_modified_blend" } }; if ($scenePath) { Write-Output "SELECTED_SCENE=$scenePath" } else { Write-Output "SELECTED_SCENE=NONE_FOUND"; exit 1 }
$targets = @("blender_yolo_generator_v2.py", "blender_solved_diagnostics.py", "copilot-continuation-handoff.md", "copilot-next-commands.md"); foreach ($t in $targets) { $hits = Get-ChildItem -Recurse -File -Filter $t -ErrorAction SilentlyContinue; if ($hits) { $hits | ForEach-Object { Write-Output ("FOUND=" + $_.FullName) } } else { Write-Output ("MISSING=" + $t) } }
Set-Location "C:\Users\abdul\Desktop\noodels"; $blenderExe = "D:\blender.exe"; if (-not (Test-Path $blenderExe)) { Write-Output "BLENDER_EXE_MISSING"; exit 1 }; $tmpDir = Join-Path (Get-Location) "automation\vision-rebuild"; $checkScript = Join-Path $tmpDir "_tmp_blender_dep_check.py"; $bootstrapScript = Join-Path $tmpDir "_tmp_blender_dep_bootstrap.py"; @'
import importlib
import json
mods = ["cv2", "numpy", "yaml"]
missing = []
for m in mods:
    try:
        importlib.import_module(m)
    except Exception as e:
        missing.append({"module": m, "error": str(e)})
print("IMPORT_CHECK=" + json.dumps({"missing": missing}))
if missing:
    raise SystemExit(2)
print("IMPORT_CHECK_OK")
'@ | Set-Content -Path $checkScript -Encoding Ascii; @'
import ensurepip
import subprocess
import sys
print("BOOTSTRAP_PIP_START")
ensurepip.bootstrap()
print("PIP_INSTALL_START")
subprocess.check_call([sys.executable, "-m", "pip", "install", "opencv-python", "numpy", "pyyaml"])
print("PIP_INSTALL_DONE")
'@ | Set-Content -Path $bootstrapScript -Encoding Ascii; & $blenderExe --background --python $checkScript; $checkExit = $LASTEXITCODE; Write-Output "IMPORT_CHECK_EXIT=$checkExit"; if ($checkExit -ne 0) { & $blenderExe --background --python $bootstrapScript; $bootExit = $LASTEXITCODE; Write-Output "BOOTSTRAP_EXIT=$bootExit"; if ($bootExit -ne 0) { Remove-Item $checkScript, $bootstrapScript -Force -ErrorAction SilentlyContinue; exit $bootExit }; & $blenderExe --background --python $checkScript; $recheckExit = $LASTEXITCODE; Write-Output "IMPORT_RECHECK_EXIT=$recheckExit"; if ($recheckExit -ne 0) { Remove-Item $checkScript, $bootstrapScript -Force -ErrorAction SilentlyContinue; exit $recheckExit } }; Remove-Item $checkScript, $bootstrapScript -Force -ErrorAction SilentlyContinue
$blenderExe = "D:\blender.exe"; $scene = "C:\Users\abdul\Desktop\noodels\scene.blend"; $diagScript = "C:\Users\abdul\Desktop\noodels\automation\vision-rebuild\blender_solved_diagnostics.py"; & $blenderExe $scene --background --python $diagScript; Write-Output "DIAGNOSTICS_EXIT=$LASTEXITCODE"
$latest = Get-ChildItem "debug-output" -Directory -Filter "solved-diagnostics-*" -ErrorAction SilentlyContinue | Sort-Object LastWriteTime -Descending | Select-Object -First 1; if (-not $latest) { Write-Output "SOLVED_DIAG_DIR_MISSING"; exit 1 }; $reportPath = Join-Path $latest.FullName "report.json"; $summaryPath = Join-Path $latest.FullName "summary.md"; if (-not (Test-Path $reportPath)) { Write-Output "REPORT_JSON_MISSING=$reportPath"; exit 1 }; if (-not (Test-Path $summaryPath)) { Write-Output "SUMMARY_MD_MISSING=$summaryPath"; exit 1 }; $report = Get-Content $reportPath -Raw | ConvertFrom-Json; $insideCount = 0; if ($report.attempts) { $insideCount = @($report.attempts | Where-Object { $_.inside_frame -eq $true }).Count }; $boardInnerExists = $report.objects.board_inner_exists; $acceptedCount = $report.summary.accepted_count; $bestOverlap = $report.summary.best_overlap_sum; $acceptMax = $report.config.SOLVED_ACCEPT_MAX_OVERLAP_SUM; $pass = ($boardInnerExists -eq $true) -and ($acceptedCount -ge 1) -and ($bestOverlap -le $acceptMax); Write-Output "LATEST_SOLVED_DIAG_DIR=$($latest.FullName)"; Write-Output "REPORT_JSON=$reportPath"; Write-Output "SUMMARY_MD=$summaryPath"; Write-Output "objects.board_inner_exists=$boardInnerExists"; Write-Output "summary.accepted_count=$acceptedCount"; Write-Output "summary.best_overlap_sum=$bestOverlap"; Write-Output "config.SOLVED_ACCEPT_MAX_OVERLAP_SUM=$acceptMax"; Write-Output "attempts.inside_frame_true_count=$insideCount"; Write-Output "SOLVED_DIAGNOSTICS_PASS=$pass"
$blenderExe = "D:\blender.exe"; $scene = "C:\Users\abdul\Desktop\noodels\scene.blend"; $runner = "C:\Users\abdul\Desktop\noodels\automation\vision-rebuild\smoke_runner_temp.py"; & $blenderExe $scene --background --python $runner; Write-Output "SMOKE_GENERATION_EXIT=$LASTEXITCODE"
$datasetRoot = "C:\Users\abdul\Desktop\noodels\debug-output\smoke-yolo-dataset"; if (-not (Test-Path $datasetRoot)) { Write-Output "SMOKE_DATASET_MISSING=$datasetRoot"; exit 1 }; $expected = [ordered]@{ train = 6; val = 2 }; $actual = [ordered]@{}; $issues = New-Object System.Collections.Generic.List[object]; $class11Count = 0; foreach ($split in @("train", "val")) { $imgDir = Join-Path $datasetRoot ("images\" + $split); $lblDir = Join-Path $datasetRoot ("labels\" + $split); $imgFiles = @(); $lblFiles = @(); if (Test-Path $imgDir) { $imgFiles = @(Get-ChildItem $imgDir -File -Filter "*.png" -ErrorAction SilentlyContinue) }; if (Test-Path $lblDir) { $lblFiles = @(Get-ChildItem $lblDir -File -Filter "*.txt" -ErrorAction SilentlyContinue) }; $actual[$split] = [ordered]@{ images = $imgFiles.Count; labels = $lblFiles.Count }; if ($imgFiles.Count -ne $expected[$split]) { $issues.Add([ordered]@{ type = "count_mismatch"; split = $split; item = "images"; expected = $expected[$split]; actual = $imgFiles.Count }) }; if ($lblFiles.Count -ne $expected[$split]) { $issues.Add([ordered]@{ type = "count_mismatch"; split = $split; item = "labels"; expected = $expected[$split]; actual = $lblFiles.Count }) }; foreach ($lbl in $lblFiles) { $lineNo = 0; foreach ($line in (Get-Content $lbl.FullName)) { $lineNo += 1; if ([string]::IsNullOrWhiteSpace($line)) { continue }; $tokens = $line.Trim().Split(" ", [System.StringSplitOptions]::RemoveEmptyEntries); if ($tokens.Count -lt 3 -or (($tokens.Count - 1) % 2 -ne 0)) { $issues.Add([ordered]@{ type = "invalid_label_format"; file = $lbl.FullName; line = $lineNo; text = $line }); continue }; $classId = 0; if (-not [int]::TryParse($tokens[0], [ref]$classId)) { $issues.Add([ordered]@{ type = "invalid_class_id"; file = $lbl.FullName; line = $lineNo; value = $tokens[0] }); continue }; if ($classId -lt 0 -or $classId -gt 11) { $issues.Add([ordered]@{ type = "class_out_of_range"; file = $lbl.FullName; line = $lineNo; value = $classId; allowed = "0..11" }) }; if ($classId -eq 11) { $class11Count += 1 }; for ($i = 1; $i -lt $tokens.Count; $i++) { $num = 0.0; $ok = [double]::TryParse($tokens[$i], [System.Globalization.NumberStyles]::Float, [System.Globalization.CultureInfo]::InvariantCulture, [ref]$num); if (-not $ok) { $issues.Add([ordered]@{ type = "non_numeric_coordinate"; file = $lbl.FullName; line = $lineNo; token = $tokens[$i] }); break }; if ($num -lt 0.0 -or $num -gt 1.0) { $issues.Add([ordered]@{ type = "coordinate_out_of_range"; file = $lbl.FullName; line = $lineNo; value = $num; allowed = "0..1" }); break } } } } }; if ($class11Count -lt 1) { $issues.Add([ordered]@{ type = "missing_board_class_11"; detail = "No class 11 board polygon labels found" }) }; $pass = ($issues.Count -eq 0); $jsonPath = Join-Path $datasetRoot "validation-report.json"; $mdPath = Join-Path $datasetRoot "validation-report.md"; $report = [ordered]@{ timestamp = (Get-Date).ToString("o"); dataset_root = $datasetRoot; expected_counts = $expected; actual_counts = $actual; class_id_allowed = "0..11"; coordinate_range_allowed = "0..1"; class11_board_label_count = $class11Count; issue_count = $issues.Count; issues = $issues; pass = $pass }; $report | ConvertTo-Json -Depth 10 | Set-Content -Path $jsonPath -Encoding Ascii; $md = New-Object System.Collections.Generic.List[string]; $md.Add("# Smoke Dataset Validation Report"); $md.Add(""); $md.Add("- Pass: $pass"); $md.Add("- Dataset root: $datasetRoot"); $md.Add("- Expected train images/labels: $($expected.train)/$($expected.train)"); $md.Add("- Expected val images/labels: $($expected.val)/$($expected.val)"); $md.Add("- Actual train images/labels: $($actual.train.images)/$($actual.train.labels)"); $md.Add("- Actual val images/labels: $($actual.val.images)/$($actual.val.labels)"); $md.Add("- Class 11 board label count: $class11Count"); $md.Add("- Issues: $($issues.Count)"); if ($issues.Count -gt 0) { $md.Add(""); $md.Add("## Issues"); foreach ($issue in $issues) { $md.Add("- " + ($issue | ConvertTo-Json -Compress)) } }; $md | Set-Content -Path $mdPath -Encoding Ascii; Write-Output "VALIDATION_JSON=$jsonPath"; Write-Output "VALIDATION_MD=$mdPath"; Write-Output "VALIDATION_PASS=$pass"; Write-Output "VALIDATION_ISSUE_COUNT=$($issues.Count)"; Write-Output "CLASS11_BOARD_LABEL_COUNT=$class11Count"; Write-Output "ACTUAL_TRAIN_IMAGES=$($actual.train.images)"; Write-Output "ACTUAL_TRAIN_LABELS=$($actual.train.labels)"; Write-Output "ACTUAL_VAL_IMAGES=$($actual.val.images)"; Write-Output "ACTUAL_VAL_LABELS=$($actual.val.labels)"
$blenderExe = "D:\blender.exe"; $scene = "C:\Users\abdul\Desktop\noodels\scene.blend"; $diagScript = "C:\Users\abdul\Desktop\noodels\automation\vision-rebuild\blender_solved_diagnostics.py"; & $blenderExe $scene --background --python $diagScript; Write-Output "DIAGNOSTICS_RERUN_EXIT=$LASTEXITCODE"
$latest = Get-ChildItem "debug-output" -Directory -Filter "solved-diagnostics-*" -ErrorAction SilentlyContinue | Sort-Object LastWriteTime -Descending | Select-Object -First 1; $reportPath = Join-Path $latest.FullName "report.json"; $summaryPath = Join-Path $latest.FullName "summary.md"; $report = Get-Content $reportPath -Raw | ConvertFrom-Json; $insideCount = @($report.attempts | Where-Object { $_.inside_frame -eq $true }).Count; $boardInnerExists = $report.objects.board_inner_exists; $acceptedCount = $report.summary.accepted_count; $bestOverlap = $report.summary.best_overlap_sum; $acceptMax = $report.config.SOLVED_ACCEPT_MAX_OVERLAP_SUM; $pass = ($boardInnerExists -eq $true) -and ($acceptedCount -ge 1) -and ($bestOverlap -le $acceptMax); Write-Output "LATEST_SOLVED_DIAG_DIR=$($latest.FullName)"; Write-Output "REPORT_JSON=$reportPath"; Write-Output "SUMMARY_MD=$summaryPath"; Write-Output "objects.board_inner_exists=$boardInnerExists"; Write-Output "summary.accepted_count=$acceptedCount"; Write-Output "summary.best_overlap_sum=$bestOverlap"; Write-Output "config.SOLVED_ACCEPT_MAX_OVERLAP_SUM=$acceptMax"; Write-Output "attempts.inside_frame_true_count=$insideCount"; Write-Output "SOLVED_DIAGNOSTICS_PASS=$pass"
$blenderExe = "D:\blender.exe"; $scene = "C:\Users\abdul\Desktop\noodels\scene.blend"; $diagScript = "C:\Users\abdul\Desktop\noodels\automation\vision-rebuild\blender_solved_diagnostics.py"; & $blenderExe $scene --background --python $diagScript; Write-Output "DIAGNOSTICS_RERUN2_EXIT=$LASTEXITCODE"
$latest = Get-ChildItem "debug-output" -Directory -Filter "solved-diagnostics-*" | Sort-Object LastWriteTime -Descending | Select-Object -First 1; $reportPath = Join-Path $latest.FullName "report.json"; $summaryPath = Join-Path $latest.FullName "summary.md"; $report = Get-Content $reportPath -Raw | ConvertFrom-Json; $insideCount = @($report.attempts | Where-Object { $_.inside_frame -eq $true }).Count; $boardInnerExists = $report.objects.board_inner_exists; $acceptedCount = $report.summary.accepted_count; $bestOverlap = $report.summary.best_overlap_sum; $acceptMax = $report.config.SOLVED_ACCEPT_MAX_OVERLAP_SUM; $pass = ($boardInnerExists -eq $true) -and ($acceptedCount -ge 1) -and ($bestOverlap -le $acceptMax); Write-Output "LATEST_SOLVED_DIAG_DIR=$($latest.FullName)"; Write-Output "REPORT_JSON=$reportPath"; Write-Output "SUMMARY_MD=$summaryPath"; Write-Output "objects.board_inner_exists=$boardInnerExists"; Write-Output "summary.accepted_count=$acceptedCount"; Write-Output "summary.best_overlap_sum=$bestOverlap"; Write-Output "config.SOLVED_ACCEPT_MAX_OVERLAP_SUM=$acceptMax"; Write-Output "attempts.inside_frame_true_count=$insideCount"; Write-Output "SOLVED_DIAGNOSTICS_PASS=$pass"
$blenderExe = "D:\blender.exe"; $scene = "C:\Users\abdul\Desktop\noodels\scene.blend"; $diagScript = "C:\Users\abdul\Desktop\noodels\automation\vision-rebuild\blender_solved_diagnostics.py"; & $blenderExe $scene --background --python $diagScript; Write-Output "DIAGNOSTICS_RERUN3_EXIT=$LASTEXITCODE"
$latest = Get-ChildItem "debug-output" -Directory -Filter "solved-diagnostics-*" | Sort-Object LastWriteTime -Descending | Select-Object -First 1; $reportPath = Join-Path $latest.FullName "report.json"; $summaryPath = Join-Path $latest.FullName "summary.md"; $report = Get-Content $reportPath -Raw | ConvertFrom-Json; $insideCount = @($report.attempts | Where-Object { $_.inside_frame -eq $true }).Count; $boardInnerExists = $report.objects.board_inner_exists; $acceptedCount = $report.summary.accepted_count; $bestOverlap = $report.summary.best_overlap_sum; $acceptMax = $report.config.SOLVED_ACCEPT_MAX_OVERLAP_SUM; $pass = ($boardInnerExists -eq $true) -and ($acceptedCount -ge 1) -and ($bestOverlap -le $acceptMax); Write-Output "LATEST_SOLVED_DIAG_DIR=$($latest.FullName)"; Write-Output "REPORT_JSON=$reportPath"; Write-Output "SUMMARY_MD=$summaryPath"; Write-Output "objects.board_inner_exists=$boardInnerExists"; Write-Output "summary.accepted_count=$acceptedCount"; Write-Output "summary.best_overlap_sum=$bestOverlap"; Write-Output "config.SOLVED_ACCEPT_MAX_OVERLAP_SUM=$acceptMax"; Write-Output "attempts.inside_frame_true_count=$insideCount"; Write-Output "SOLVED_DIAGNOSTICS_PASS=$pass"
$blenderExe = "D:\blender.exe"; $scene = "C:\Users\abdul\Desktop\noodels\scene.blend"; $runner = "C:\Users\abdul\Desktop\noodels\automation\vision-rebuild\smoke_runner_temp.py"; & $blenderExe $scene --background --python $runner; Write-Output "SMOKE_GENERATION_RERUN_EXIT=$LASTEXITCODE"
$datasetRoot = "C:\Users\abdul\Desktop\noodels\debug-output\smoke-yolo-dataset"; if (-not (Test-Path $datasetRoot)) { Write-Output "SMOKE_DATASET_MISSING=$datasetRoot"; exit 1 }; $expected = [ordered]@{ train = 6; val = 2 }; $actual = [ordered]@{}; $issues = New-Object System.Collections.Generic.List[object]; $class11Count = 0; foreach ($split in @("train", "val")) { $imgDir = Join-Path $datasetRoot ("images\" + $split); $lblDir = Join-Path $datasetRoot ("labels\" + $split); $imgFiles = @(); $lblFiles = @(); if (Test-Path $imgDir) { $imgFiles = @(Get-ChildItem $imgDir -File -Filter "*.png" -ErrorAction SilentlyContinue) }; if (Test-Path $lblDir) { $lblFiles = @(Get-ChildItem $lblDir -File -Filter "*.txt" -ErrorAction SilentlyContinue) }; $actual[$split] = [ordered]@{ images = $imgFiles.Count; labels = $lblFiles.Count }; if ($imgFiles.Count -ne $expected[$split]) { $issues.Add([ordered]@{ type = "count_mismatch"; split = $split; item = "images"; expected = $expected[$split]; actual = $imgFiles.Count }) }; if ($lblFiles.Count -ne $expected[$split]) { $issues.Add([ordered]@{ type = "count_mismatch"; split = $split; item = "labels"; expected = $expected[$split]; actual = $lblFiles.Count }) }; foreach ($lbl in $lblFiles) { $lineNo = 0; foreach ($line in (Get-Content $lbl.FullName)) { $lineNo += 1; if ([string]::IsNullOrWhiteSpace($line)) { continue }; $tokens = $line.Trim().Split(" ", [System.StringSplitOptions]::RemoveEmptyEntries); if ($tokens.Count -lt 3 -or (($tokens.Count - 1) % 2 -ne 0)) { $issues.Add([ordered]@{ type = "invalid_label_format"; file = $lbl.FullName; line = $lineNo; text = $line }); continue }; $classId = 0; if (-not [int]::TryParse($tokens[0], [ref]$classId)) { $issues.Add([ordered]@{ type = "invalid_class_id"; file = $lbl.FullName; line = $lineNo; value = $tokens[0] }); continue }; if ($classId -lt 0 -or $classId -gt 11) { $issues.Add([ordered]@{ type = "class_out_of_range"; file = $lbl.FullName; line = $lineNo; value = $classId; allowed = "0..11" }) }; if ($classId -eq 11) { $class11Count += 1 }; for ($i = 1; $i -lt $tokens.Count; $i++) { $num = 0.0; $ok = [double]::TryParse($tokens[$i], [System.Globalization.NumberStyles]::Float, [System.Globalization.CultureInfo]::InvariantCulture, [ref]$num); if (-not $ok) { $issues.Add([ordered]@{ type = "non_numeric_coordinate"; file = $lbl.FullName; line = $lineNo; token = $tokens[$i] }); break }; if ($num -lt 0.0 -or $num -gt 1.0) { $issues.Add([ordered]@{ type = "coordinate_out_of_range"; file = $lbl.FullName; line = $lineNo; value = $num; allowed = "0..1" }); break } } } } }; if ($class11Count -lt 1) { $issues.Add([ordered]@{ type = "missing_board_class_11"; detail = "No class 11 board polygon labels found" }) }; $pass = ($issues.Count -eq 0); $jsonPath = Join-Path $datasetRoot "validation-report.json"; $mdPath = Join-Path $datasetRoot "validation-report.md"; $report = [ordered]@{ timestamp = (Get-Date).ToString("o"); dataset_root = $datasetRoot; expected_counts = $expected; actual_counts = $actual; class_id_allowed = "0..11"; coordinate_range_allowed = "0..1"; class11_board_label_count = $class11Count; issue_count = $issues.Count; issues = $issues; pass = $pass }; $report | ConvertTo-Json -Depth 10 | Set-Content -Path $jsonPath -Encoding Ascii; $md = New-Object System.Collections.Generic.List[string]; $md.Add("# Smoke Dataset Validation Report"); $md.Add(""); $md.Add("- Pass: $pass"); $md.Add("- Dataset root: $datasetRoot"); $md.Add("- Expected train images/labels: $($expected.train)/$($expected.train)"); $md.Add("- Expected val images/labels: $($expected.val)/$($expected.val)"); $md.Add("- Actual train images/labels: $($actual.train.images)/$($actual.train.labels)"); $md.Add("- Actual val images/labels: $($actual.val.images)/$($actual.val.labels)"); $md.Add("- Class 11 board label count: $class11Count"); $md.Add("- Issues: $($issues.Count)"); if ($issues.Count -gt 0) { $md.Add(""); $md.Add("## Issues"); foreach ($issue in $issues) { $md.Add("- " + ($issue | ConvertTo-Json -Compress)) } }; $md | Set-Content -Path $mdPath -Encoding Ascii; Write-Output "VALIDATION_JSON=$jsonPath"; Write-Output "VALIDATION_MD=$mdPath"; Write-Output "VALIDATION_PASS=$pass"; Write-Output "VALIDATION_ISSUE_COUNT=$($issues.Count)"; Write-Output "CLASS11_BOARD_LABEL_COUNT=$class11Count"; Write-Output "ACTUAL_TRAIN_IMAGES=$($actual.train.images)"; Write-Output "ACTUAL_TRAIN_LABELS=$($actual.train.labels)"; Write-Output "ACTUAL_VAL_IMAGES=$($actual.val.images)"; Write-Output "ACTUAL_VAL_LABELS=$($actual.val.labels)"
$blenderExe = "D:\blender.exe"; $scene = "C:\Users\abdul\Desktop\noodels\scene.blend"; $latestDiag = Get-ChildItem "debug-output" -Directory -Filter "solved-diagnostics-*" | Sort-Object LastWriteTime -Descending | Select-Object -First 1; $diagReportPath = Join-Path $latestDiag.FullName "report.json"; $diagSummaryPath = Join-Path $latestDiag.FullName "summary.md"; $diag = Get-Content $diagReportPath -Raw | ConvertFrom-Json; $insideCount = @($diag.attempts | Where-Object { $_.inside_frame -eq $true }).Count; $diagPass = ($diag.objects.board_inner_exists -eq $true) -and ($diag.summary.accepted_count -ge 1) -and ($diag.summary.best_overlap_sum -le $diag.config.SOLVED_ACCEPT_MAX_OVERLAP_SUM); $smokeRoot = "C:\Users\abdul\Desktop\noodels\debug-output\smoke-yolo-dataset"; $valJsonPath = Join-Path $smokeRoot "validation-report.json"; $valMdPath = Join-Path $smokeRoot "validation-report.md"; $val = Get-Content $valJsonPath -Raw | ConvertFrom-Json; Write-Output "BLENDER_EXE=$blenderExe"; Write-Output "SCENE_SELECTED=$scene"; Write-Output "DIAG_DIR=$($latestDiag.FullName)"; Write-Output "DIAG_REPORT=$diagReportPath"; Write-Output "DIAG_SUMMARY=$diagSummaryPath"; Write-Output "DIAG_objects.board_inner_exists=$($diag.objects.board_inner_exists)"; Write-Output "DIAG_summary.accepted_count=$($diag.summary.accepted_count)"; Write-Output "DIAG_summary.best_overlap_sum=$($diag.summary.best_overlap_sum)"; Write-Output "DIAG_config.SOLVED_ACCEPT_MAX_OVERLAP_SUM=$($diag.config.SOLVED_ACCEPT_MAX_OVERLAP_SUM)"; Write-Output "DIAG_attempts.inside_frame_true_count=$insideCount"; Write-Output "DIAG_PASS=$diagPass"; Write-Output "SMOKE_ROOT=$smokeRoot"; Write-Output "SMOKE_VALIDATION_JSON=$valJsonPath"; Write-Output "SMOKE_VALIDATION_MD=$valMdPath"; Write-Output "SMOKE_PASS=$($val.pass)"; Write-Output "SMOKE_ISSUE_COUNT=$($val.issue_count)"; Write-Output "SMOKE_CLASS11_BOARD_LABEL_COUNT=$($val.class11_board_label_count)"
git status --short -- blender_yolo_generator_v2.py automation/vision-rebuild/smoke_runner_temp.py; git diff -- blender_yolo_generator_v2.py automation/vision-rebuild/smoke_runner_temp.py
git rev-parse --abbrev-ref HEAD; git status --short -- blender_yolo_generator_v2.py automation/vision-rebuild/smoke_runner_temp.py
Get-History | Select-Object -ExpandProperty CommandLine
